[build]
  command = "npm --prefix frontend run build"
  publish = "frontend/dist"
  base = "."

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
